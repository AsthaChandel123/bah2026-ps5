# models/ — AI prediction core dependencies (ISRO BAH 2026 PS5)
# =============================================================
# The ensemble is TIERED so it always runs: the numpy/stdlib members
# (baselines, ridge, analog, stacking + EMOS + conformal) need only numpy.
# Heavier members are OPTIONAL and skipped gracefully if their library is
# missing (see models/README.md "Robustness").

# ---- Required (always) -------------------------------------------------
numpy>=1.24            # arrays, all baselines/ridge/analog/ensemble math

# ---- Strongly recommended ---------------------------------------------
scipy>=1.10            # (optional) numerical helpers; not strictly required
pandas>=2.0            # (optional) convenience; core path does not import it

# ---- Optional: gradient boosting (first available backend is used) -----
# Priority: xgboost -> lightgbm -> scikit-learn HistGradientBoosting/RF.
xgboost>=1.7           # OPTIONAL — preferred per-cell boosting backend
lightgbm>=4.0          # OPTIONAL — alternative boosting backend
scikit-learn>=1.3      # OPTIONAL — HistGradientBoosting / RandomForest fallback

# ---- Optional: deep learning (spatiotemporal members) ------------------
torch>=2.0             # OPTIONAL — ConvLSTM nowcaster + U-Net field model (CPU OK)

# ---- Optional: classical statistics ------------------------------------
statsmodels>=0.14      # OPTIONAL — SARIMAX/AR references (analog+AR(1) cover the floor)

# Install the always-on core only:        pip install numpy
# Install everything (CPU):                pip install -r models/requirements.txt
# CPU-only torch:  pip install --index-url https://download.pytorch.org/whl/cpu torch
